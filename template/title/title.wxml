<template name="title">
  <view class="title">
    <text>{{title}}</text>
    <navigator class="more" hover-class="none" wx:if="{{url}}" url="{{url}}">
      更多 >
    </navigator>
  </view>
</template>
